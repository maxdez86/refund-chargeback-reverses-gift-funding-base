{
  "schemaVersion": 1,
  "scoring": {
    "itemScale": { "missing": 0, "partial": 1, "correct": 2 },
    "unsupportedClaimPenalty": 2,
    "criticalFailureRule": "Any omitted or materially incorrect critical item is a critical failure.",
    "sourceRule": "Graph EXTRACTED edges may guide discovery. INFERRED or AMBIGUOUS claims require exact source verification to receive credit."
  },
  "tasks": {
    "api-flow": {
      "items": [
        { "id": "handler", "critical": false, "expect": "apps/api/src/functions/payments-create/handler.ts parses the HTTP request and delegates to PaymentService." },
        { "id": "domain", "critical": false, "expect": "apps/api/src/domain/payment-service.ts validates CreatePaymentRequestSchema, coordinates gift/payment state, vendor checkout creation, persistence, and parses CreatePaymentResponseSchema." },
        { "id": "services", "critical": false, "expect": "Identify directly used DynamoDB repository and Asaas/payment infrastructure collaborators without inventing runtime dependencies." },
        { "id": "contracts", "critical": false, "expect": "packages/contracts/src/payments.ts owns CreatePaymentRequestSchema and CreatePaymentResponseSchema and their nested payment schemas." },
        { "id": "response", "critical": false, "expect": "Explain the handler response construction and error boundary from exact source." },
        { "id": "tests", "critical": false, "expect": "Cite directly relevant payments-create handler and PaymentService unit tests." }
      ]
    },
    "contract-consumers": {
      "items": [
        { "id": "api-payment-service", "critical": true, "expect": "apps/api/src/domain/payment-service.ts directly consumes CreatePaymentRequestSchema and CreatePaymentResponseSchema." },
        { "id": "api-message-service", "critical": true, "expect": "apps/api/src/domain/payment-message-service.ts directly consumes CreatePaymentMessageRequestSchema and CreatePaymentMessageResponseSchema." },
        { "id": "api-discard-service", "critical": true, "expect": "apps/api/src/domain/payment-discard-service.ts directly consumes DiscardPaymentResponseSchema." },
        { "id": "web-payments-api", "critical": true, "expect": "apps/web/src/lib/payments-api.ts directly consumes the create, get, discard, and message response schemas; its request references are types, not request-schema runtime consumers." },
        { "id": "unit-test", "critical": true, "expect": "apps/api/tests/payment-discard-handler.test.ts directly consumes DiscardPaymentResponseSchema." },
        { "id": "integration-script", "critical": true, "expect": "scripts/test-prod-promotion-integration.ts directly consumes CreatePaymentResponseSchema, GetPaymentResponseSchema, and DiscardPaymentResponseSchema." },
        { "id": "classification", "critical": true, "expect": "Separate production API, production web, test, and script consumers and exclude packages/contracts/src/payments.ts declarations/internal composition." }
      ]
    },
    "web-flow": {
      "items": [
        { "id": "ui", "critical": false, "expect": "apps/web/src/components/sections/Presentes.tsx uses giftsQueryKey and getGifts through query state and invalidation." },
        { "id": "adapter", "critical": false, "expect": "apps/web/src/lib/gifts-api.ts fetches the gifts endpoint and validates GetGiftsResponseSchema." },
        { "id": "route", "critical": false, "expect": "infra/cdk/lib/stacks/app-stack.ts wires the gifts-get Lambda HTTP route." },
        { "id": "handler", "critical": false, "expect": "apps/api/src/functions/gifts-get/handler.ts calls GiftService.getGifts and enqueueExpiryTrigger." },
        { "id": "domain", "critical": false, "expect": "apps/api/src/domain/gift-service.ts reads the repository and validates GetGiftsResponseSchema." },
        { "id": "contract", "critical": false, "expect": "packages/contracts/src/gifts.ts owns GetGiftsResponseSchema." },
        { "id": "tests", "critical": false, "expect": "Cite focused Presentes, gifts-api, and GiftService tests." }
      ]
    },
    "cdk-dependencies": {
      "items": [
        { "id": "props", "critical": false, "expect": "Map AppStackProps inputs including certificate, domain, table, stage, secrets/config, and environment." },
        { "id": "resources", "critical": false, "expect": "Map the HTTP API, Lambda functions/routes, queues/DLQs, schedules, permissions, logs, and policies constructed by AppStack." },
        { "id": "outputs", "critical": false, "expect": "Map AppStack public members consumed by ObservabilityStack and any CloudFormation outputs." },
        { "id": "composition", "critical": false, "expect": "infra/cdk/bin/app.ts passes CertificateStack and DataStack values into AppStack and passes AppStack values into ObservabilityStack." },
        { "id": "dependency-kind", "critical": false, "expect": "Do not attribute edgeStack.addDependency(certificateStack) to AppStack; distinguish explicit dependencies from construct-reference data flow." }
      ]
    },
    "opentofu-owner": {
      "items": [
        { "id": "owner", "critical": true, "expect": "infra/opentofu/api-dns owns the API DNS record." },
        { "id": "resource", "critical": true, "expect": "infra/opentofu/api-dns/main.tf resource cloudflare_dns_record.api creates an unproxied CNAME using var.api_domain." },
        { "id": "target", "critical": true, "expect": "local.api_target trims the ApiCustomDomainRegionalTarget output from data.aws_cloudformation_stack.app and supplies resource.content." },
        { "id": "variables", "critical": true, "expect": "Identify app_stack_name, API domain, AWS stage/region/profile, Cloudflare zone/token inputs from the module and wrapper." },
        { "id": "commands", "critical": true, "expect": "package.json opentofu:api-dns commands call scripts/landing-opentofu.sh; scripts/deploy-backend-and-sync-asaas-webhook.sh initializes and applies the module." }
      ]
    },
    "rename-plan": {
      "items": [
        { "id": "declaration", "critical": true, "expect": "packages/config/src/index.ts exports resourceName and stageResourceName delegates to it." },
        { "id": "bin-callers", "critical": true, "expect": "infra/cdk/bin/app.ts contains direct calls." },
        { "id": "stack-callers", "critical": true, "expect": "Direct callers exist in app-stack.ts, data-stack.ts, edge-stack.ts, and observability-stack.ts." },
        { "id": "tests", "critical": true, "expect": "Direct test callers exist in packages/config/tests/index.test.ts and infra/cdk/test/edge-stack.test.ts." },
        { "id": "classification", "critical": true, "expect": "AGENTS.md mentions are instructions, not direct code callers; no direct caller is invented for PlatformStack or unrelated files." },
        { "id": "validation", "critical": false, "expect": "Recommend focused config/CDK tests followed by lint, typecheck, and unit tests without the prod-promotion integration suite." }
      ]
    }
  }
}
